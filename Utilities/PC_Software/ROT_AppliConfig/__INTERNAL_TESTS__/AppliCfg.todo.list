setob:
    if begin == "wrpgrp1" or begin == "wrpgrp2":
        # When using .bat files the variable needs to be set
        pattern=output.pattern(begin, "=")
        #TODO miss a parameter (protected_area ?)
        
modify_file_value:
    TODO : should return 3 values :change, not necessary, not found
    
    